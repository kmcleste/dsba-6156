version: "3.8"

services:
  api:
    image: kam2897/search-api:latest
    ports:
      - "8000:8000"

  ui:
    image: kam2897/search-ui:latest
    ports:
      - "8501:8501"
    environment:
      - API_BASE_URL=http://search-api:8000
    depends_on:
      - api
