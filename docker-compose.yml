version: "3.8"

services:
  api:
    image: api:latest
    ports:
      - "8000:8000"
    network_mode: bridge

  ui:
    image: ui:latest
    ports:
      - "8501:8501"
    depends_on:
      - api
    network_mode: bridge
